# StudyBloom 测试清单

## 自动化检查

```bash
npm run lint
npm run test
npm run build
```

生产 PWA 预览：

```bash
npm run build
npm run preview
```

## iPhone Safari

使用正式 HTTPS 域名测试：

1. Safari 打开网站。
2. 点击分享 → 添加到主屏幕。
3. 检查桌面图标和名称是否为 StudyBloom。
4. 从主屏幕打开，确认没有 Safari 地址栏和底部工具栏。
5. 检查刘海屏顶部、Home Indicator 底部没有遮挡。
6. 注册或登录 Supabase 账号。
7. 新增任务、完成任务、刷新页面，确认数据同步。
8. 断开网络重新打开，确认基础应用壳和离线提示可以显示。
9. 恢复网络后确认提示自动消失，重新操作任务成功。
10. 发布新版本后确认出现更新提示；选择“稍后”不会立即刷新，选择“立即更新”后加载新版本。

## Android Chrome

检查原生“安装 StudyBloom”提示、主屏幕图标、standalone 窗口和刷新后的数据同步。

## 普通浏览器和内置浏览器

- 桌面 Chrome、Safari、Edge：不应受 PWA 安装提示影响。
- 微信、QQ 内置浏览器：显示使用 Safari 或系统浏览器打开的提示。
- 登录、日历、统计、设置和密码重置路由刷新不应出现 Vercel 404。

## 网络异常

- 断网时显示“当前处于离线状态，云端数据暂时不可用”。
- 在线恢复时显示“网络已恢复，可以继续同步计划”。
- 离线新增、编辑、删除任务不显示虚假的保存成功提示。

## 好友功能

前置条件：Supabase 已执行好友系统迁移，准备账号 A、B、C 并各自记录 StudyBloom ID。

### 添加与申请

1. A 通过 B 的 StudyBloom ID 搜索，只看到头像、昵称和 ID，可发送申请。
2. C 的好友页面不应看到 A 与 B 之间的申请。
3. B 可以接受申请；用 A 的界面尝试操作 B 收到的申请应无入口，构造请求也被 RLS 拒绝。
4. 重复发送申请、用错误/模糊 ID 搜索应给出友好中文提示。
5. B 拒绝后，A 可再次发送；B 拉黑 A 后，A 无法再发送申请。
6. B 在“设置 → 好友与隐私”关闭“允许接收好友申请”后，新申请被拒绝。

### 日历授权与只读

7. A、B 成为好友后，默认互相看不到对方日历（显示“该好友暂未向你开放日历。”）。
8. A 在设置中为 B 开启“允许查看我的日历”后，B 才能看到 A 的月历、任务完成情况、当月总数与完成率。
9. C 始终无法查看 A 的日历。
10. B 的好友日历页没有任何添加、编辑、删除、勾选、休息日或备注入口，并有“好友日历 · 只读”标识。
11. 在浏览器开发者工具中直接构造请求：读取可成功，但对 A 的任务执行 INSERT/UPDATE/DELETE（或篡改 `user_id`/`requester_id`）应被 RLS 拒绝。
12. A 关闭授权后，B 立即无法读取；A 删除好友 B 后，B 也无法读取。
13. 删除或拉黑后，SQL Editor 中确认 `calendar_shares` 对应记录已被触发器清除。

### 界面回归

14. 手机端底部导航为“日历/好友/统计/设置”四项，触控高度正常，不被 Home Indicator 遮挡。
15. 电脑端好友日历复用“日期行 + 任务行”布局，七列对齐，只读标识明显。
16. 好友功能不影响本人日历、统计、导入导出和 PWA 功能。

## 学习模块

前置条件：Supabase 已按顺序执行 `20260811000000_add_attendance_and_study_mode.sql`、`20260812000000_harden_study_data_integrity.sql`、`20260813000000_add_task_study_goals_and_reflections.sql`、`20260814000000_add_daily_study_goal.sql` 与 `20260815000000_add_countdown_and_friend_notes.sql`，设备定位服务可用。

### 地点与签到

1. 设置 → 学习地点：新增地点「学校图书馆」（半径 200 米），需先点击「使用当前位置」获取坐标；名称为空或半径超出 100–1000 被阻止。
2. 经纬度默认折叠在「高级信息」中，不默认展示。
3. 修改中心位置、停用地点前均有二次确认；停用后地点仍在列表中（历史签到记录保留），不能签到。
4. 学习页选择地点 → 签到：成功显示地点名、签到时间、在场时长、定位精度与距离中心距离。
5. 在允许范围外签到：提示「你距离「学校图书馆」约 N 米，允许签到范围为 200 米。」（前端预检 + 数据库复核一致）。
6. 定位权限被拒绝：中文提示包含「系统设置 → 隐私与安全性 → 定位服务」；系统定位关闭、超时、精度 >150 米各有对应中文提示。
7. 同一时间只能有一条未签退记录：已签到时签到入口消失，出现签退按钮。
8. 正常签退需在地点范围内；「异常结束本次记录」需二次确认，记录标记异常结束且不出现在有效在场时长里。
9. 一天可多次签到/签退；最近签到记录不显示任何历史坐标。

### 自由计时与番茄专注

10. 开始自由计时 → 暂停（暂停期间时长不增长）→ 继续 → 结束；结束后打开学习小结，正确显示本次时长、番茄轮数和今日累计。可单独保存学习感受，也可勾选同步完成关联任务，任务不会被自动完成。
11. 从日历页任务行点「开始学习」跳转到 /study?task=…，该任务自动选中；直接打开学习页时默认选中今日第一项未完成任务，自由学习仍可手动选择。
12. 番茄专注：默认 25+5、长休息 15、每 4 轮；预设「标准专注 25+5」「深度学习 50+10」；专注 15–90 / 短休息 3–30 / 长休息 10–60 / 间隔 2–8 轮之外无法保存；偏好在另一设备登录后同步。
13. 专注倒计时归零：前台提示「本轮专注已完成，休息一下吧。」（可选轻提示音/振动），进入等待状态；手动开始休息/跳过休息/开始下一轮；只有完整专注轮计入完成轮数。
14. 专注进行中暂停：剩余时间冻结；继续后倒计时从剩余时间恢复。
15. 提前结束本轮：实际专注分钟计入学习时长，完成轮数不增加。
16. 休息阶段不产生学习时长；统计页番茄专注时长 = 各专注片段之和。

### 恢复与多设备

17. 学习进行中刷新页面 / 切后台 / 锁屏后恢复：计时与阶段状态完整恢复，不重复创建会话。
18. 番茄专注进行中关闭页面，超过阶段结束时间后再打开：片段准确结束在计划时间，轮数 +1，进入等待状态并提示休息。
19. 账号 B 登录不能看到账号 A 的地点、签到、学习会话；直接构造请求读写对方数据被 RLS 拒绝（见 supabase/README.md）。
20. 两个设备同时尝试开始学习/签到：数据库保证只产生一个活动会话/一条未签退记录，后到者收到中文提示或返回已有记录。

### 统计与备份

21. 统计页今天/本周/本月切换：总时长、日均、次数、最长单次、自由/番茄时长、完成轮数、近七天柱形图、按任务统计均正确；无任务但有学习记录时仍显示学习时长。
22. 跨午夜学习（23:00–01:00）在两天各计 1 小时。
23. 设置导出全部计划（version 2），新账号导入后任务、地点、签到、学习会话、片段、番茄偏好与关联关系完整恢复；version 1 旧备份仍可导入。
24. 自由计时 40 秒 → 暂停 → 再学习 40 秒后结束，会话应标记完成；两段总和不足 60 秒时标记取消。
25. 23:50 开始并在次日完成的番茄，完成轮数归入次日；迁移前历史轮数总数保持不变。
26. 断网后开始、暂停、继续、结束、签到、签退和番茄阶段按钮均不可操作，并显示中文离线说明；恢复网络后自动刷新学习状态。
27. 使用登录会话直接对 `attendance_records`、`study_sessions`、`study_session_segments` 发起写请求均被拒绝，正常 RPC 操作仍成功。

### 界面回归

28. 320–430px 宽度下底部五列导航完整显示，ActiveStudyBar 位于导航上方且不遮挡内容；桌面端 ActiveStudyBar 在右下角。
29. 所有计时文字（01:25:36 / 18:42）在 320px 不溢出；所有按钮中文 aria-label 可读。
30. 新建任务可不设置预计时长，也可使用 30/45/60/90/120 分钟快捷值或输入 1–1440 分钟；复制日期、复制月份和完整备份恢复后预计时长不丢失。
31. 任务学习进度按该任务关联的全部有效计时片段累计，达到或超过目标后进度条停在 100%，文字仍显示真实累计时长。
32. 「学习详情」显示累计时长、有学习片段的会话次数、最近学习时间和最近 10 条记录；删除任务后，学习页和统计中的任务名称快照及历史时长仍保留。
33. 学习小结输入 500 字可保存，501 字被阻止；手机输入法弹出时内容可滚动且保存按钮可操作。
34. V0.4.1 及更早导出的 version 2 备份缺少 `estimatedMinutes` / `reflection` 时仍可导入，分别按未设置和空内容处理。
35. 设置每日目标为 120 分钟后，首页和统计页的今日时长一致；学习满 120 分钟显示 100% 以上的真实完成率、满进度条和柔和完成提示。
36. 关闭每日目标后，首页和统计页显示“未开启”，学习时长仍正常累计；重新开启后保留设置的分钟数。
37. 今天/本周/本月切换时，自由学习与番茄专注占比、按任务时长、每日番茄轮数、有效签到天数、目标达成天数和学习时间段随范围正确变化。
38. 本周复盘展示本周总时长、相对上周变化、投入最多的任务、本周计划任务完成数、目标达成天数和固定规则生成的温暖总结；不产生任何 AI 或第三方请求。
39. 分别导出学习会话、每日统计和签到 CSV，使用 Excel 或 Numbers 打开中文不乱码；签到文件只包含地点、时间、距离和结果，不含经纬度。
40. 导入 V0.6.0 之前、缺少 `dailyGoalEnabled` / `dailyGoalMinutes` 的 version 2 备份时，默认恢复为开启的 120 分钟目标。
41. 设置中填写倒计时名称和日期并开启，首页正确显示剩余自然日；目标当天和过去日期均不显示负数，关闭后首页卡片消失。
42. 倒计时设置在另一台设备登录后保持一致；V0.7.0 之前的 version 2 备份默认恢复为关闭倒计时。
43. 账号 A 为好友 B 添加备注后，A 的好友列表、日历共享设置和 B 的好友日历标题优先显示备注；清空后恢复 B 的公开昵称。
44. 账号 B 无法查询 A 写给自己的备注；A 不能给非好友写备注，超过 30 个字符被前端和数据库共同拒绝。
45. 删除或拉黑好友后，SQL Editor 中对应双方的 `friend_notes` 均被清理；重新建立好友关系不会恢复旧备注。
46. 320–430px 宽度下倒计时卡片、备注编辑输入框和操作按钮均无横向滚动，触控区域不小于 44px。

## V0.8.0 首次体验与快速开始

47. 新账号没有任务和学习记录时，首页出现两步首次引导；选择快捷模板后仍可修改名称和预计分钟数，关闭或“稍后开始”后刷新不重复弹出。
48. 已有任意任务、已完成学习记录或活动会话的账号不显示首次引导；清空浏览器 localStorage 也不会把老用户误判为新用户。
49. 今日有多项任务时，首页选择排序最前的未完成任务；连续快速点击“开始学习”只产生一个会话。
50. 没有保存偏好时快速开始使用 25 分钟专注、5 分钟休息；保存过自由计时或番茄偏好后沿用最近模式与参数。
51. 已有活动会话时首页显示任务快照、模式和时间；暂停会话必须点击“继续学习”才恢复，前台或 PWA 从后台返回不会自动创建第二个会话。
52. 今日无任务时可从四个模板或自定义内容创建任务；任务创建成功但计时失败时任务仍保留，并显示中文错误。
53. 完成有效学习、累计三个有效学习日、首次完成全天任务后，依次只显示一个渐进提示；关闭后七天不再出现，地点提示不会主动请求定位。
54. 好友页分享生成当前域名的邀请链接；支持系统分享时打开分享面板，不支持时复制链接。
55. 未登录打开邀请链接会先保存邀请码并进入登录页；登录后回到好友确认页面，不会自动发送好友申请。
56. 邀请页正确显示昵称和 StudyBloom ID；邀请自己、已经是好友、已发出申请、收到申请和拉黑状态均显示对应中文状态。
57. 只有点击“发送好友申请”后才提交；成为好友后日历仍默认关闭，必须由所有者在设置页单独授权。
58. 在 320×568、375×667、375×812、390×844、393×852、414×896、430×932 和桌面 1440px 下检查：首页无横向滚动，今日学习主按钮可见，七列月历完整，首次引导可滚动且底部导航不遮挡内容。

## V0.9.0 一起绽放

前置条件：执行 `20260816000000_add_companionship.sql`，准备已接受好友 A/B、非好友 C，并让 A/B 分别产生跨零点前后的学习片段。

59. A/B 成为好友后默认 `none`，双方首页都不能看到学习分钟、任务数量或有效学习状态。
60. A 对 B 选择 `bloom_only` 后，B 只能获得 A 每天是否达到 10 分钟，不得从响应中得到分钟数、任务数、任务标题、会话时间、备注或位置。
61. A 改为 `summary` 后，B 可看到整数分钟和任务完成数量，但仍不能读取原始学习会话或签到数据。
62. A 关闭分享后，B 刷新即失去概要；日历共享状态保持不变。
63. C 调用 A 的搭子概要和周记 RPC 被拒绝；A 拉黑 B 后，B 同样被拒绝，双方设置和小花被清理。
64. 双方均至少开启 `bloom_only` 且同一中国自然日各有 10 分钟有效片段时，该日显示共同绽放；暂停和番茄休息不计入。
65. 仅勾选任务或学习不足 10 分钟不形成共同绽放，页面不显示失败、断签、落后或催促文案。
66. A 对 B 快速连点、多标签页和多设备同时送花，只产生一条当天记录；中国时间零点后可再次送花。
67. 双人周记只展示共同天数、互送小花天数、累计共同天数和里程碑，不出现双方时长或完成率比较。
68. 陪伴者模式进入首页时搭子卡优先，但仍可正常访问自己的任务和开始学习。
69. 删除首页搭子或好友关系失效后，首页回到正确空状态；单人日历、计时、统计、签到、倒计时和导出不受影响。
70. 在 320、375、390、393、414、430px 下检查搭子卡七个日期状态、设置下拉框和送花按钮无横向滚动，触控区域不小于 44px，PWA 安全区域无遮挡。
