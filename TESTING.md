# StudyBloom 测试清单

## 自动化检查

```bash
npm run lint
npm run test
npm run build
```

自动化测试覆盖 UTC+8 深夜、月末、跨年、闰年、空数据、连续打卡、休息日和导入数据中的用户 ID 忽略规则。

## 认证

- 注册新邮箱：显示验证邮件提示或直接进入日历，取决于 Supabase 设置。
- 正确邮箱和密码：成功进入日历。
- 错误密码或不存在账号：显示中文错误，不泄露内部信息。
- 重复点击登录：请求期间按钮不可再次提交。
- 刷新页面：保持登录状态和当前账号数据。
- 未登录访问 `/calendar`：自动跳转登录页。
- 已登录访问 `/login`：自动跳转日历。
- 忘记密码：收到邮件并可在 `/reset-password` 设置新密码。
- 退出登录：回到登录页，受保护页面不可访问。

## 任务与日期

- 新增、编辑、删除、勾选、取消勾选和上下排序。
- 空任务不可添加，任务最多 100 字。
- 删除前出现二次确认。
- 多任务时月历最多显示三项，其余显示数量。
- 月初、月末、跨年、闰年二月的补位日期正确。
- UTC+8 23:30 左右新建计划，日期不提前或延后。
- 切换月份时保留旧内容直到新数据返回，并显示加载层。
- 设置和取消休息日，保存备注。
- 复制前一天，复制到目标日期，分别检查追加和覆盖。
- 网络断开时显示中文错误，乐观更新失败后恢复原状态。

## 统计

- 任务总数、已完成数、完成率、有计划天数和全部完成天数正确。
- 空数据库显示 0，不出现 NaN。
- 休息日不增加连续天数，也不中断连续记录。
- 未来日期不参与统计。
- 最近七天在手机宽度下保持七列且不横向溢出。

## 导入导出

- 导出文件名包含 `StudyBloom` 和当前本地日期。
- 错误 JSON、错误版本、无效日期、空标题和超大文件被拒绝。
- 导入前展示任务和日期设置数量。
- 追加模式保留原数据，覆盖模式只覆盖导入文件涉及的日期。
- 导入文件中的 `user_id` 被忽略，数据库写入当前登录用户 ID。

## RLS 双账号测试

按照 [supabase/README.md](supabase/README.md) 创建账号 A 和 B。分别验证不能跨账号读取、修改、删除或伪造 `user_id`。正常结果为空结果或权限错误；任何跨账号成功都视为阻塞上线的问题。

## 手机端验收

在浏览器设备模式使用 375 x 812：

- 页面无横向滚动。
- 日期格和按钮触控区域至少约 44px。
- 底部导航不遮挡内容。
- 每日编辑器从底部打开，可滚动且关闭按钮可触达。
- 输入法弹出时表单仍可操作。
- 登录、日历、统计和设置文字保持可读。

## 上线前检查

- `npm run lint`、`npm run test`、`npm run build` 全部成功。
- 仓库与构建日志不存在真实密钥或 `service_role`。
- Supabase 两张表均开启 RLS，八条策略存在。
- 正式 Site URL 和 Redirect URLs 已配置。
- 使用两个真实账号完成隔离测试。
- 在 Vercel 正式域名测试刷新子路由和密码重置。
