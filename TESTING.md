# StudyBloom 测试清单

## 自动化检查

```bash
npm run lint
npm run test
npm run build
```

生产 PWA 预览：

```bash
npm run build
npm run preview
```

## iPhone Safari

使用正式 HTTPS 域名测试：

1. Safari 打开网站。
2. 点击分享 → 添加到主屏幕。
3. 检查桌面图标和名称是否为 StudyBloom。
4. 从主屏幕打开，确认没有 Safari 地址栏和底部工具栏。
5. 检查刘海屏顶部、Home Indicator 底部没有遮挡。
6. 注册或登录 Supabase 账号。
7. 新增任务、完成任务、刷新页面，确认数据同步。
8. 断开网络重新打开，确认基础应用壳和离线提示可以显示。
9. 恢复网络后确认提示自动消失，重新操作任务成功。
10. 发布新版本后确认出现更新提示；选择“稍后”不会立即刷新，选择“立即更新”后加载新版本。

## Android Chrome

检查原生“安装 StudyBloom”提示、主屏幕图标、standalone 窗口和刷新后的数据同步。

## 普通浏览器和内置浏览器

- 桌面 Chrome、Safari、Edge：不应受 PWA 安装提示影响。
- 微信、QQ 内置浏览器：显示使用 Safari 或系统浏览器打开的提示。
- 登录、日历、统计、设置和密码重置路由刷新不应出现 Vercel 404。

## 网络异常

- 断网时显示“当前处于离线状态，云端数据暂时不可用”。
- 在线恢复时显示“网络已恢复，可以继续同步计划”。
- 离线新增、编辑、删除任务不显示虚假的保存成功提示。

## 好友功能

前置条件：Supabase 已执行好友系统迁移，准备账号 A、B、C 并各自记录 StudyBloom ID。

### 添加与申请

1. A 通过 B 的 StudyBloom ID 搜索，只看到头像、昵称和 ID，可发送申请。
2. C 的好友页面不应看到 A 与 B 之间的申请。
3. B 可以接受申请；用 A 的界面尝试操作 B 收到的申请应无入口，构造请求也被 RLS 拒绝。
4. 重复发送申请、用错误/模糊 ID 搜索应给出友好中文提示。
5. B 拒绝后，A 可再次发送；B 拉黑 A 后，A 无法再发送申请。
6. B 在“设置 → 好友与隐私”关闭“允许接收好友申请”后，新申请被拒绝。

### 日历授权与只读

7. A、B 成为好友后，默认互相看不到对方日历（显示“该好友暂未向你开放日历。”）。
8. A 在设置中为 B 开启“允许查看我的日历”后，B 才能看到 A 的月历、任务完成情况、当月总数与完成率。
9. C 始终无法查看 A 的日历。
10. B 的好友日历页没有任何添加、编辑、删除、勾选、休息日或备注入口，并有“好友日历 · 只读”标识。
11. 在浏览器开发者工具中直接构造请求：读取可成功，但对 A 的任务执行 INSERT/UPDATE/DELETE（或篡改 `user_id`/`requester_id`）应被 RLS 拒绝。
12. A 关闭授权后，B 立即无法读取；A 删除好友 B 后，B 也无法读取。
13. 删除或拉黑后，SQL Editor 中确认 `calendar_shares` 对应记录已被触发器清除。

### 界面回归

14. 手机端底部导航为“日历/好友/统计/设置”四项，触控高度正常，不被 Home Indicator 遮挡。
15. 电脑端好友日历复用“日期行 + 任务行”布局，七列对齐，只读标识明显。
16. 好友功能不影响本人日历、统计、导入导出和 PWA 功能。

## 学习模块

前置条件：Supabase 已执行 `migrations/20260811000000_add_attendance_and_study_mode.sql`，设备定位服务可用。

### 地点与签到

1. 设置 → 学习地点：新增地点「学校图书馆」（半径 200 米），需先点击「使用当前位置」获取坐标；名称为空或半径超出 100–1000 被阻止。
2. 经纬度默认折叠在「高级信息」中，不默认展示。
3. 修改中心位置、停用地点前均有二次确认；停用后地点仍在列表中（历史签到记录保留），不能签到。
4. 学习页选择地点 → 签到：成功显示地点名、签到时间、在场时长、定位精度与距离中心距离。
5. 在允许范围外签到：提示「你距离「学校图书馆」约 N 米，允许签到范围为 200 米。」（前端预检 + 数据库复核一致）。
6. 定位权限被拒绝：中文提示包含「系统设置 → 隐私与安全性 → 定位服务」；系统定位关闭、超时、精度 >150 米各有对应中文提示。
7. 同一时间只能有一条未签退记录：已签到时签到入口消失，出现签退按钮。
8. 正常签退需在地点范围内；「异常结束本次记录」需二次确认，记录标记异常结束且不出现在有效在场时长里。
9. 一天可多次签到/签退；最近签到记录不显示任何历史坐标。

### 自由计时与番茄专注

10. 开始自由计时 → 暂停（暂停期间时长不增长）→ 继续 → 结束；结束后询问是否完成关联任务，两个选项都可用，任务不会被自动完成。
11. 从日历页任务行点「开始学习」跳转到 /study?task=…，该任务自动选中；任务下显示「今日已学习 X 分钟」。
12. 番茄专注：默认 25+5、长休息 15、每 4 轮；预设「标准专注 25+5」「深度学习 50+10」；专注 15–90 / 短休息 3–30 / 长休息 10–60 / 间隔 2–8 轮之外无法保存；偏好在另一设备登录后同步。
13. 专注倒计时归零：前台提示「本轮专注已完成，休息一下吧。」（可选轻提示音/振动），进入等待状态；手动开始休息/跳过休息/开始下一轮；只有完整专注轮计入完成轮数。
14. 专注进行中暂停：剩余时间冻结；继续后倒计时从剩余时间恢复。
15. 提前结束本轮：实际专注分钟计入学习时长，完成轮数不增加。
16. 休息阶段不产生学习时长；统计页番茄专注时长 = 各专注片段之和。

### 恢复与多设备

17. 学习进行中刷新页面 / 切后台 / 锁屏后恢复：计时与阶段状态完整恢复，不重复创建会话。
18. 番茄专注进行中关闭页面，超过阶段结束时间后再打开：片段准确结束在计划时间，轮数 +1，进入等待状态并提示休息。
19. 账号 B 登录不能看到账号 A 的地点、签到、学习会话；直接构造请求读写对方数据被 RLS 拒绝（见 supabase/README.md）。
20. 两个设备同时尝试开始学习/签到：数据库保证只产生一个活动会话/一条未签退记录，后到者收到中文提示或返回已有记录。

### 统计与备份

21. 统计页今天/本周/本月切换：总时长、日均、次数、最长单次、自由/番茄时长、完成轮数、近七天柱形图、按任务统计均正确；无任务但有学习记录时仍显示学习时长。
22. 跨午夜学习（23:00–01:00）在两天各计 1 小时。
23. 设置导出全部计划（version 2），新账号导入后任务、地点、签到、学习会话、片段、番茄偏好与关联关系完整恢复；version 1 旧备份仍可导入。

### 界面回归

24. 320–430px 宽度下底部五列导航完整显示，ActiveStudyBar 位于导航上方且不遮挡内容；桌面端 ActiveStudyBar 在右下角。
25. 所有计时文字（01:25:36 / 18:42）在 320px 不溢出；所有按钮中文 aria-label 可读。
